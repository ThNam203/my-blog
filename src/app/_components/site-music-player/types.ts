export type SiteMusicPlayerLabels = {
    play: string;
    pause: string;
    expand: string;
    collapse: string;
    previous: string;
    next: string;
    minimize: string;
    restore: string;
};
